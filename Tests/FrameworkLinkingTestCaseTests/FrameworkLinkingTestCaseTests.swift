import XCTest
@testable import FrameworkLinkingTestCase

class FrameworkLinkingTestCaseTests: XCTestCase {

    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssert(swiftFunc())
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
